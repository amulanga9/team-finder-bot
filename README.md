# 🚀 Telegram Bot для поиска teammates

**Production-ready** телеграм-бот для поиска со-основателей и команд в стартап-акселераторе Launch Lab.

## 🎯 Production Features

- ✅ **Layered Architecture** - Services, Schemas, Middlewares
- ✅ **Automatic Session Cleanup** - Context managers для БД
- ✅ **Graceful Shutdown** - Signal handlers (SIGTERM, SIGINT)
- ✅ **Rate Limiting** - Защита от спама (20 req/min)
- ✅ **Background Tasks** - Автоматическая очистка expired invitations и inactive users
- ✅ **Database Optimization** - Indexes, constraints, CASCADE deletes
- ✅ **Pydantic Settings** - Валидация конфигурации
- ✅ **Docker Ready** - Production Dockerfile + docker-compose
- ✅ **Logging** - Консоль + файлы с ротацией
- ✅ **Health Checks** - Для Docker и мониторинга

## ✨ Возможности

### Для команд (2+ человека)
- 📝 Регистрация команды с названием и идеей
- 🔍 Поиск соискателей по навыкам
- 💌 Отправка приглашений (до 5/день)
- 📊 Статистика команды и запросы

### Для соло-основателей
- 💡 Регистрация с идеей и навыком
- 🤝 Поиск других соло с алгоритмом совместимости (⭐1-5)
- 💬 Отправка запросов на коллаборацию
- 📈 Трекинг статуса запросов

### Для соискателей
- 👤 Регистрация с навыками (1-3)
- 📱 Tinder-style поиск команд
- ✅ Свайп влево/вправо по командам
- 🔔 Уведомления о приглашениях

## 🛠 Технологии

- **Python 3.11+**
- **aiogram 3.4.1** - Telegram Bot framework
- **PostgreSQL** - База данных
- **SQLAlchemy 2.0.25** - ORM с оптимизацией
- **asyncpg 0.29.0** - Async PostgreSQL driver
- **Pydantic 2.5.0** - Валидация и Settings
- **cachetools 5.3.2** - TTL cache для rate limiting
- **Docker** - Containerization
- **Alembic 1.13.1** - Database migrations (опционально)

## 📋 Требования

- Python 3.11 или выше
- PostgreSQL 12 или выше
- Telegram Bot Token (от @BotFather)

## 🚀 Быстрый старт (Docker - РЕКОМЕНДУЕТСЯ)

### 1. Клонировать репозиторий

```bash
git clone https://github.com/yourusername/exams_21.git
cd exams_21
```

### 2. Настроить переменные окружения

```bash
cp bot/.env.example bot/.env
nano bot/.env  # Добавьте BOT_TOKEN и DB_PASSWORD
```

### 3. Запустить через Docker

```bash
docker-compose up -d
```

Готово! Бот запущен и работает в фоновом режиме.

Проверить логи:
```bash
docker-compose logs -f bot
```

---

## 📦 Установка без Docker (Manual)

### 1. Клонировать репозиторий

```bash
git clone https://github.com/yourusername/exams_21.git
cd exams_21
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установить зависимости

```bash
cd bot
pip install -r requirements.txt
```

### 4. Настроить переменные окружения

Создайте файл `.env` в папке `bot/`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/teammates_db
```

### 5. Создать базу данных

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE teammates_db;
CREATE USER teammates_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE teammates_db TO teammates_user;
```

### 6. Запустить бота

```bash
cd bot
python main.py
```

Вы должны увидеть:

```
INFO - Запуск бота...
INFO - Создание таблиц в базе данных...
INFO - Таблицы успешно созданы
INFO - Бот успешно запущен
```

## 📝 Команды бота

- `/start` - Регистрация и создание профиля
- `/profile` - Показать профиль и статистику
- `/search` - Поиск teammates
- `/team` - Статистика команды (только для лидеров)
- `/cancel` - Отменить текущее действие
- `/help` - Показать справку

## 🏗 Структура проекта (Production-Ready)

```
exams_21/
├── bot/
│   ├── database/
│   │   ├── __init__.py
│   │   ├── db.py              # Подключение к БД + context managers
│   │   ├── models.py          # SQLAlchemy модели (с indexes)
│   │   └── crud.py            # CRUD операции
│   ├── services/              # 🆕 Бизнес-логика (будущее)
│   │   └── __init__.py
│   ├── schemas/               # 🆕 Pydantic схемы (будущее)
│   │   └── __init__.py
│   ├── middlewares/           # 🆕 Middleware компоненты
│   │   ├── __init__.py
│   │   └── throttling.py      # Rate limiting
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── start.py           # Регистрация
│   │   ├── search.py          # Поиск teammates
│   │   ├── invitations.py     # Обработка приглашений
│   │   ├── profile.py         # Профиль пользователя
│   │   ├── team.py            # Статистика команды
│   │   └── commands.py        # /help, /cancel
│   ├── keyboards/
│   │   ├── __init__.py
│   │   ├── inline.py          # Инлайн клавиатуры
│   │   └── reply.py           # Reply клавиатуры
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── texts.py           # Тексты сообщений
│   │   └── states.py          # FSM состояния
│   ├── config.py              # 🆕 Pydantic Settings (валидация)
│   ├── tasks.py               # 🆕 Background tasks (cleanup)
│   ├── main.py                # 🆕 Точка входа (с graceful shutdown)
│   ├── requirements.txt       # Зависимости
│   └── .env.example           # 🆕 Расширенный пример .env
├── Dockerfile                 # 🆕 Production Dockerfile
├── docker-compose.yml         # 🆕 Docker Compose конфигурация
├── .dockerignore              # 🆕 Docker ignore rules
├── README.md
├── PROMPTS.md                 # История промптов
└── DEPLOYMENT.md              # 🆕 Обновленные инструкции
```

## 🔧 Разработка

### Добавление новых обработчиков

1. Создайте файл в `bot/handlers/`
2. Создайте Router:

```python
from aiogram import Router

router = Router()

@router.message(Command("mycommand"))
async def my_handler(message: Message):
    await message.answer("Hello!")
```

3. Подключите в `main.py`:

```python
from handlers.myhandler import router as my_router

dp.include_router(my_router)
```

## 🐛 Отладка

### Логи

Логи выводятся в консоль. Уровень логирования настраивается в `main.py`:

```python
logging.basicConfig(
    level=logging.INFO,  # Измените на DEBUG для детальных логов
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

### Частые проблемы

**Ошибка подключения к БД:**
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь что PostgreSQL запущен
- Проверьте права пользователя БД

**Бот не отвечает:**
- Проверьте `BOT_TOKEN` в `.env`
- Убедитесь что токен валидный (получен от @BotFather)
- Проверьте интернет-соединение

## 📊 База данных

### Модели

- **User** - Пользователи (команды, соло, соискатели)
- **Team** - Команды
- **Invitation** - Приглашения и запросы

## 🚀 Деплой

См. [DEPLOYMENT.md](DEPLOYMENT.md) для подробных инструкций по деплою на:
- VPS (Ubuntu/Debian)
- Docker
- Heroku
- Railway

## 📄 Лицензия

MIT License

## 📞 Поддержка

- Issues: [GitHub Issues](https://github.com/yourusername/exams_21/issues)

---

Сделано с ❤️ для Launch Lab
